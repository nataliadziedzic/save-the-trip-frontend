import styled from 'styled-components'

export const StyledItem = styled.li`
  margin: 4px 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
  .MuiSvgIcon-root {
    font-size: 18px;
  }
  .button {
    margin: 0 8px;
  }
  .bought .MuiSvgIcon-root {
    fill: #06da31;
  }
`
